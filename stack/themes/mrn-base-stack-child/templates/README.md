Add site-specific custom templates here when new entities or page templates are needed.

Examples:
- `templates/template-landing.php`
- `templates/single-resource.php`
- `templates/template-rollout-starter.php`

Keep shared runtime logic in stack layers; keep child templates focused on site-specific rendering.
