# Cookie Consent 1.1.45 optional-plugin release

Release unit: `mrn-cookie-consent/mrn-cookie-consent.php`, upgrade-only and active-state preserving. No full-Stack lock, theme, GTM Injector or other plugin changes.

- Source: `mrnwebdesigns/mrn-cookie-consent`, merged PRs #2 and #3, commit `f939c903ab6f44ad2c1373761242205fd2d067c5`.
- Package: `zip/mrn-cookie-consent.zip`, version 1.1.45, 62,161 bytes.
- SHA-256: `59aa760ea99d34d9b5eb084247ce1c2ec650fb9ff6cc001a7bf1972c8fa498a5`.
- Adds explicit accessible names to the bundled preference switches, using their existing visible category legends. Fixes invalid branding link semantics and WordPress's localized `"0"` diagnostic flag. Privileged AJAX capability/nonce checks remain intact. No public diagnostic endpoint or consent-enforcement rewrite.
- MRN QA: full plugin static and strict release QA pass on clean merged source and unpacked release ZIP. Focused coverage includes 80 branding cases, 24 diagnostic flag/transport cases, real WordPress roles, expired nonces, cached HTML, keyboard/Axe, consent persistence/revocation and tracker blocking. Standalone optional scope excludes full-Stack parity/rollout checks; missing Semgrep/npm/Composer/PHPUnit configurations are documented in the QA packet.

## Upgrade qualification

Re-resolve each exact site URL in MainWP and fresh-sync only that site. Require this plugin already installed, an older supported version, preserved active state, exact forward and installed rollback packages, checksum-locked optional-plugin preflight and a fresh verified remote database-only backup. Missing installations are not eligible. Never pass an empty site list.

The optional-plugin path uses MainWP Child's authenticated `installplugintheme` operation. It does **not** require the separate Stack deployment agent; do not install that agent merely for this update. Respect MCP safe mode and its confirmation-token flow.

For a pre-1.1.43 site, inspect the raw `mrn_silktide_consent_settings.enabled` value. Absent or malformed settings stay disabled in newer releases. If an existing default-enabled banner should remain visible, obtain explicit site-scope authorization and use the release's operator-run `scripts/enable-reviewed-legacy-banner.php` before upgrading. It checks exact home URL and the raw option checksum, refuses explicit/malformed choices, sets only `enabled=1` and preserves other fields. Back up before this settings write and again before the plugin update. Do not silently enable previously disabled sites.

## Tharrington Smith canary

Exact target: `https://tharringtonsmith.com/`, verified MainWP site 94. Re-resolve the exact URL on each later operation. The 1.1.44 canary exposed a pre-existing bundled-vendor checkbox name defect; 1.1.45 corrects it. The final update from active 1.1.44 to active **1.1.45** succeeded at **2026-09-23 15:50:43 UTC** through MainWP's upgrade-only preflight and confirmation flow, preserving activation. No Stack deployment agent was installed. Persistent safe mode and user confirmation remained enabled; the narrowly authorized session closed after the update.

Immediately before the update, labeled database-only Updraft backup `d00fa061889b`, `pre-cookie-consent-1.1.45-approved-20260923`, was verified complete and uploaded to the existing isolated S3 destination. Its receipt was consumed by the update. The exact 1.1.44 rollback ZIP is SHA-256 `a14427f81b0802cf20584a2e78cff65e3001a6ea1fc37be2b12c847b77256bc0`; the original 1.1.37 recovery package is also retained privately (`a423e869b8ff6a4539cf71609a05aabb0476bb73a97a26bcee5011ccd9749abc`).

Fresh targeted MainWP sync completed at 15:51:12 UTC (the connector's 30-second timeout was resolved by readback, without repeating the update). MainWP and direct readback show active 1.1.45. All seven release files match the package hashes; SiteGround's generated minified CSS is a derived runtime file. Caches were purged. The earlier authorized migration preserves the formerly default-enabled banner with raw settings `{"enabled":1}`.

Live homepage and Contact at 1440, 768 and 390 pixels: 12 settled banner/modal Axe scans with zero cookie findings, valid noninteractive empty branding, named preference switches, keyboard/Escape, viewport containment and no JavaScript page errors. Desktop/mobile Accept, Reject, custom preferences, persistence/reload and revocation pass. Administrator diagnostics return 200; visitors and nonadmins make no automatic request. Explicit unauthorized or stale-nonce requests remain rejected. No public diagnostic endpoint was added.

The package is a verified single-site canary and a **fleet release candidate**, not a blanket fleet GO. The 24-hour warmed-cache observation remains due. Full-site MRN QA reports unrelated theme/form/frame accessibility, reCAPTCHA storage-access console errors and homepage CLS around 0.62. The generic immediate Axe scan can catch the cookie fade transition; the settled cookie scans above pass. Site Kit loads its Google tag before consent, so the site's zero-pre-consent-tracker gate is not met. Keep wider rollout on hold pending that integration decision and per-site qualification; do not change Site Kit, themes or WPForms as part of this release.

## Rollback

Use the exact prior package, a fresh verified database backup and MainWP's checksum-locked optional-plugin rollback preflight/confirmation. Preserve activation and purge HTML/assets. For an immediate rollback, verify 1.1.44 and its captured file hashes, consent controls and options. The saved `enabled=1` preserves the old visible behavior and normally stays in place; restoring the formerly absent raw option is a separate explicit settings rollback, never an automatic database restore. An immediate 1.1.44 rollback retains the two original fixes but restores the unnamed bundled preference switches. A separately reviewed rollback to 1.1.37 restores both original defects.

## Fleet readiness limits

The release is an optional plugin unit, not a full-Stack promotion. Qualify existing installations by their actual settings, vendor asset source, active state and integration profile. The bundled vendor label repair does not rewrite independently hosted custom vendor URLs. Exercise those custom assets explicitly.

Before the first wider cohort, repeat the canary after approximately 24 hours with warmed caches, inspect current runtime logs, then explicitly authorize a small exact-site cohort. Resolve every site's URL, sync, capture exact rollback files, verify a fresh labeled remote database backup, preflight and use the MainWP confirmation flow. Stop on any version/hash/activation mismatch, broken consent, new plugin Axe finding or unexpected diagnostic write. Never select all sites implicitly.

Tharrington Smith's broader site QA is not green: existing theme/form accessibility findings and homepage layout shift remain. Site Kit independently loads its Google tag before consent; the plugin's early denied Consent Mode default remains present, and plugin-controlled tracker fixtures pass. This release does not establish zero site-wide tracking requests. Those integration and site issues require separate scoped work; no theme, WPForms, Site Kit or unrelated plugin was changed.

QA evidence: strict MRN component release checks and focused real-WordPress tests; 80 branding scenarios, 24 diagnostic transport/flag scenarios, administrator/subscriber/editor/anonymous authorization, cached/expired nonces, consent persistence and revocation; direct English/French switch names/keyboard/persistence and settled Axe at desktop/mobile. Four pre-existing vendor console messages are manually confirmed debug-flag guarded and disabled by default; the generic debug-artifact scan's file-specific exclusion is documented. Missing package-specific Semgrep/Composer/npm/PHPUnit configuration and full-Stack-only rows are intentionally not applicable. Engine public-login smoke is replaced by the explicit four-role integration coverage.
